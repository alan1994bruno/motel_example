# Conecta no banco manual que acabamos de subir
spring.datasource.url=jdbc:postgresql://localhost:5433/motel_test_db
spring.datasource.username=testuser
spring.datasource.password=testpass
spring.driver-class-name=org.postgresql.Driver

# Recria o banco a cada teste para garantir limpeza
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Token fixo para testes
api.security.token.secret=chave_super_secreta_para_testes